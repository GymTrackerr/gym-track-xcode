//
//  Client.swift
//  GymTracker
//
//  Created by Daniel Kravec on 2025-10-01.
//

import Foundation
import SwiftData
import Combine

final class Client: ObservableObject {
/*
 @Published var splitDays: [SplitDay] = []

 private var container:ModelContainer
 

 init() {
     let configuration = ModelConfiguration(isStoredInMemoryOnly: true, allowsSave: false)
     
     do {
         container = try ModelContainer(
             for: SplitDay.self,
             configurations: configuration
         )
     } catch {
         fatalError("Failed to create ModelContainer: \(error)")
     }
 }
 
 // Split Days
 public func getSplitDays() {
     let context = container.mainContext
     var descriptor = FetchDescriptor<SplitDay>()
     descriptor.includePendingChanges = true
     
     do {
         let results = try context.fetch(descriptor)
         self.splitDays = results
     } catch {
         print("Failed to fetch SplitDay: \(error)")
     }
 }
 
 public func addSplitDay(SplitDay: SplitDay) {
     splitDays.append(SplitDay)
     container.)
 }
 
 private func removeSplitDay(SplitDay: SplitDay) {
     
 }
 */
    
    //
}
